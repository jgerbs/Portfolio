@{
    ViewData["Title"] = "Error";
    var message = ViewBag.ErrorMessage ?? "An unexpected error occurred.";
}

<div class="text-center mt-5">
    <h2 class="text-danger">Oops!</h2>
    <p>@message</p>
    <a class="btn btn-primary mt-3" href="/">Return Home</a>
</div>
